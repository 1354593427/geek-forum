# 站点 URL（部署后填写，用于 sitemap/RSS）
# SITE_URL=https://your-username.github.io/geek-forum

# 后端 API 根地址（为空时使用纯静态模式：posts.json + localStorage）
# 阶段 2/4：设为 http://localhost:3080/api 可连本地 API（草稿/回收站/收藏需 token）
# VITE_API_BASE=

# 阶段 C：后端写操作后是否自动 git commit（便于 Push 到 GitHub Pages）
# 在运行后端的项目根目录下生效，需为 git 仓库。设为 1 或 true 开启
# GIT_AUTO_COMMIT=0

# Giscus 评论（在 https://giscus.app 配置后填入）
# 需在仓库启用 Discussions 并安装 giscus app
# VITE_GISCUS_REPO=owner/repo
# VITE_GISCUS_REPO_ID=
# VITE_GISCUS_CATEGORY=Comments
# VITE_GISCUS_CATEGORY_ID=
